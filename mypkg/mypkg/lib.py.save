import pandas as pd
import numpy as np
from os import path

def clean_data():
  laredoute_df.drop(laredoute_df.columns(1))
  laredoute_df = laredoute_df.reset_index()
  print(laredoute_df.head())

clean_data()
